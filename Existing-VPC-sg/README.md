# Day 30 – Security Group in Existing VPC

## What this does

Creates a new security group inside an already existing VPC.

## Key concept

In real projects we use existing VPCs instead of creating new ones.

## Commands

terraform init
terraform apply
terraform destroy